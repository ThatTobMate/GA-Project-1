<p id="notice"><%= notice %></p>
<p><%= @album.title %></p>
<% @album.pictures.each do |picture| %>

<%= picture.caption %>
<%= picture.description %>
<%= image_tag picture.image %>

<% end %>






<%= link_to 'Edit', edit_profile_album_path(@album.profile.id, @album.id) %> |
<%= link_to 'Back', profile_albums_path %>